import {MatchResult} from './MatchResult'

// A TUPLE TYPE !!!
// a very practical use of Tuple - so we can describe the types in this list
export type MatchData = [Date, string, string, number, number, MatchResult, string];